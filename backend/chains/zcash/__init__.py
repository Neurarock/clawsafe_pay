"""
chains.zcash — Zcash chain support.

**Status: placeholder** — stubs only.

Dependencies (not yet installed):
    pip install zcash  # or a Zcash RPC wrapper

Key differences from Bitcoin:
    - Fork of Bitcoin with added privacy features
    - Transparent addresses (t-addr): similar to Bitcoin P2PKH/P2SH
    - Shielded addresses (z-addr): zs… (Sapling) or unified addresses (u…)
    - Native asset: ZEC (8 decimals, like BTC)
    - Supports shielded transactions (zk-SNARKs)
    - Memo field built into shielded outputs (512 bytes)
"""
